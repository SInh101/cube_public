{
  "name": "@rubiks-learning/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace @rubiks-learning/cube-core && tsc --noEmit",
    "dev": "tsx watch src/local/startLocalServer.ts",
    "typecheck": "tsc --noEmit --pretty false"
  },
  "dependencies": {
    "@rubiks-learning/api-contract": "0.0.0",
    "@rubiks-learning/cube-core": "0.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0"
  }
}
