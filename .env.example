# Browser-visible base URL of the deployed REST API (no trailing slash).
# Leave empty when web and API share an origin.
VITE_API_BASE_URL=

# Reserved for the REST API's later Supabase integration.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
